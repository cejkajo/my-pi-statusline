# Source notice

## Statusline

The statusline began as [`leohenon/my-pi-statusline`](https://github.com/leohenon/my-pi-statusline), which was built on [`nicobailon/pi-powerline-footer`](https://github.com/nicobailon/pi-powerline-footer). This repository's Git history retains that inherited work and the later `cejkajo/my-pi-statusline` changes.

The Pi 0.84.0 repair also imports intended newer statusline changes from the read-only disabled dotfiles copy at `home/.pi/agent/disabled/my-pi-statusline`, while preserving this repository's later session, Git, Gondolin, context-usage, and ANSI behavior.

The inherited histories and package metadata did not contain a license grant.

## Usage limits

`usage-limits.ts`, `USAGE-LIMITS.md`, and `test/usage-limits.test.ts` were imported through the disabled dotfiles copy. That copy records their origin as the working tree of `cejkajo/pi-usage-limits` at base commit `2d1c9d1c95b417b825e50b9044f68c65cdeb4a87`, including then-uncommitted changes.

## Permission

The repository owner has confirmed permission to copy, modify, and publish these sources in the owner's repositories. This notice records provenance and that permission. It does not grant a general license to third parties.
